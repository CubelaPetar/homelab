---
main_uid: "0"
main_gid: "{{ main_uid }}"
docker_compose_generator_output_path: "/root"
containers:
  - service_name: xandikos
    active: true
    image: ghcr.io/jelmer/xandikos
    ports:
      - 8000:8000
    volumes:
      - ./data:/data
    restart: unless-stopped
